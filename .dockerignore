# Ignore heavy or sensitive files when building the Docker image context.
node_modules
dist
.env
.env.local
.git
.github
*.log
.DS_Store

# Frontend build artifacts (Dockerfile's `frontend` stage rebuilds these
# from source; local copies would only slow the context transfer).
web-next/.next
web-next/out
src/web-next
